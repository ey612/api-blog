services:
  - type: web
    name: my-blog
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
```
> **개념**: Render에게 "이렇게 실행해줘!"라고 알려주는 설정 파일

**③ `.gitignore`** - GitHub에 올리지 말아야 할 파일 목록
```
__pycache__/
*.pyc
.env
venv/